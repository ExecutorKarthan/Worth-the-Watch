{{!-- //{{#if logged_in}} --}}
    <section class="flex-row justify-center">
        {{#each results as |result|}}
            {{> list-movies}}
        {{/each}}
        <a href="/add-movie">Don't see your movie?</a>
    </section>
{{!-- {{/if}} --}}